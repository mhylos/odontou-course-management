interface CoursePreviewProps {
  // course: Course;
  // onCourseClick: (course: Course) => void;
}

export default function CoursePreview({}: CoursePreviewProps) {
  return <div className="bg-white shadow-lg rounded-lg overflow-hidden"></div>;
}
